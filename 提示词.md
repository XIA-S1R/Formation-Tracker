**实物地图仿真**

这是一个“实物地图 + 仿真动力学”的接入前验证版本。

主启动文件是：

src/simulation/launch/tracking_real_field_sim_triangle.launch

它保留三机追踪算法链路，但地图换成了你手工测量生成的真实场地地图，target 仍然走仿真动力学。

整体链路是：

static_pointcloud_map_publisher
  -> /global_map

real_field_evasion.py
  -> /target/position_cmd
  -> target/so3_controller
  -> target/so3_quadrotor
  -> /target/odom

/droneX/target_dpf_sim_node
  yolo input: /target/odom
  odom input: /droneX/odom
  map input: /global_map
  output: /droneX/target_ekf_odom

/droneX/planning.launch
  target input: /droneX/target_ekf_odom
  map input: /droneX/gridmap_inflate
  output: /droneX/position_cmd

/droneX/uav_simulator_only.launch
  /droneX/position_cmd
  -> so3_controller
  -> so3_quadrotor
  -> /droneX/odom
关键点：

这不是最终实机 launch，而是实机前验证 launch。
target 仍然是仿真的，且由 real_field_evasion.py 离线发布 /target/position_cmd。
三架追踪机现在也还是仿真动力学，只是地图、起点、target 轨迹都按实物场地来。
后续真机时，替换的是三架追踪机的 uav_simulator_only.launch，变成真实无人机 odom 回传 + /droneX/position_cmd 下发给机载端。
DPF、planning、mapping_real、静态地图发布器这些逻辑可以继续留在 PC 端。
我刚改过的内容：

target 起点：(0.00, 2.10, 1.20)
drone0 起点：(0.00, 1.15, 1.20)
drone1 起点：(-0.55, 1.85, 1.20)
drone2 起点：(0.55, 1.85, 1.20)
target 轨迹文件：src/planning/planning/scripts/real_field_evasion.py
轨迹是非闭环的，约 32.5s，会依次绕过三个障碍物。
预览图：src/real_uav_bridge/maps/generated/real_field_evasion_preview.png

roslaunch simulation tracking_real_field_sim_triangle.launch rviz:=true planner_visualization:=true

如果你要用这条仿真链路录制 bag，建议只录命令话题，不录 `/triger`，这样回放时不会被历史 trigger 干扰：

rosbag record -O real_field_sim_position_cmd.bag \
  /drone0/position_cmd /drone1/position_cmd /drone2/position_cmd

这里不需要 remap，因为仿真和真机都使用同名的 `/droneX/position_cmd`。如果你只想先做单机回放，也可以只录：

rosbag record -O drone0_position_cmd.bag /drone0/position_cmd

真机回放时直接播放这个 bag 即可，建议先暂停：

rosbag play --pause real_field_sim_position_cmd.bag

等机载端起飞悬停、你手动打开 `/triger` 之后，再按空格开始播放。不要对这种普通轨迹 bag 再开 `--loop`，除非你确认轨迹首尾是平滑闭合的。

****实物地图实飞**

**通信验证
**

先按单机 drone0 做，成功后复制到 drone1/drone2。

机载端：

roslaunch real_uav_bridge onboard_position_cmd_receiver_px4.launch
  drone_id:=0
  ground_ip:=<PC_IP>
  cmd_listen_port:=18000
  ground_port:=18100
  mocap_listen_port:=5005
  mocap_tracker:=<drone0_tracker_name>
  start_mavros:=false
  start_px4ctrl:=false

PC 端启动 ground bridge：

rosrun real_uav_bridge cmd_odom_ground_bridge
  __ns:=/drone0
  __name:=position_cmd_sender
  _drone_ip:=<UAV0_IP>
  _cmd_port:=18000
  _listen_port:=18100
  _cmd_topic:=position_cmd
  _odom_topic:=udp_odom
  _trigger_topic:=traj_start_trigger
  _send_before_enable:=true

动捕电脑需要把同一个刚体数据分别发给 PC 和对应机载端：

python mocap_udp_relay.py --tracker <drone0_tracker_name> --target <PC_IP>:5005
python mocap_udp_relay.py --tracker <drone0_tracker_name> --target <UAV0_IP>:5005

PC 上确认 PC 直接收到动捕：

roslaunch real_uav_bridge mocap_udp_odom_triangle.launch

  drone0_port:=5005
  drone0_tracker:=<drone0_tracker_name>

rostopic hz /drone0/odom

机载端确认本地动捕 odom：

rostopic hz /mocap/odom
rostopic echo -n1 /mocap/odom

PC 端确认“机载收到 odom 后又回传给 PC”：

rostopic hz /drone0/udp_odom
rostopic echo -n1 /drone0/udp_odom

比较 **/drone0/odom** 和 **/drone0/udp_odom** 的位置，应基本一致。

PC -> 机载 command 验证：

rostopic pub -r 5 /drone0/position_cmd quadrotor_msgs/PositionCommand "header:
  frame_id: 'world'
position: {x: 0.0, y: 1.15, z: 1.0}
velocity: {x: 0.0, y: 0.0, z: 0.0}
acceleration: {x: 0.0, y: 0.0, z: 0.0}
jerk: {x: 0.0, y: 0.0, z: 0.0}
yaw: 0.0
yaw_dot: 0.0
kx: [0.0, 0.0, 0.0]
kv: [0.0, 0.0, 0.0]
trajectory_id: 1
trajectory_flag: 1"机载端确认：

rostopic hz /tracker_cmd/position_cmd
rostopic echo -n1 /tracker_cmd/position_cmd

ready trigger 回传验证：

rostopic pub -1 /traj_start_trigger geometry_msgs/PoseStamped "header:
  stamp: now
  frame_id: 'world'
pose:
  orientation:
    w: 1.0"

PC 端确认：

rostopic echo -n1 /drone0/traj_start_trigger

**单机定点 command 接管试飞**

这一段是控制方式验证，不是单纯通信验证。新的 onboard 控制节点仍然叫 **/px4ctrl**，但实际运行的是 **px4_position_executor**：启动后会自动预发送 setpoint、切 **OFFBOARD**、arm，并按 27com 风格起飞到启动点局部坐标 **[0, 0, 1.0]**。节点内部会把它映射成 PX4 local 下的 **px4_local_origin + [0, 0, 1.0]**，所以即使 **/mavros/local_position/pose** 起点不是 0，也不应该因为照发 0,0 而横移。不要再按旧 px4ctrl 流程手动起飞后拨 5/6 通道。

先只做 drone0。PC 端启动 ground bridge，保持 command gate 关闭：

rosrun real_uav_bridge cmd_odom_ground_bridge
  __ns:=/drone0
  __name:=position_cmd_sender
  _drone_ip:=<UAV0_IP>
  _cmd_port:=18000
  _listen_port:=18100
  _cmd_topic:=position_cmd
  _odom_topic:=udp_odom
  _trigger_topic:=traj_start_trigger
  _enable_topic:=/triger
  _send_before_enable:=false

机载端先做不会起飞的链路预检查，确认 odom relay 和 MAVROS local position 正常：

cd /home/xia_s1r/Documents/FormaitionVSTracker/Formation-Tracker-Onboard
source devel/setup.bash

roslaunch real_uav_bridge onboard_position_cmd_receiver_px4.launch
  drone_id:=0
  ground_ip:=<PC_IP>
  cmd_listen_port:=18000
  ground_port:=18100
  mocap_listen_port:=5005
  mocap_tracker:=<drone0_tracker>
  start_mavros:=true
  start_px4ctrl:=false
  relay_odom_to_mavros:=true
  fcu_url:=/dev/ttyTHS0:921600

预检查项：

rostopic echo /mavros/state
rostopic hz /mocap/odom
rostopic echo -n1 /mocap/local_origin
rostopic hz /mavros/odometry/out
rostopic hz /mavros/local_position/pose
rosrun tf tf_echo odom camera_init
rosrun tf tf_echo body base_link

预期现象：

1. onboard launch 按 27com 的方式把 **/mocap/odom** 转成启动点局部 odom 后发到 **/mavros/odometry/out**，同时发布锁存的 **/mocap/local_origin**。
2. 静态 TF 是 **map -> odom -> camera_init** 和 **body -> base_link**，PX4/MAVROS 正常后 **/mavros/local_position/pose** 应该有数据。
3. 预检查阶段 **/mavros/setpoint_raw/local** 可以没有数据，因为此时没有启动执行器。

如果 **/mavros/local_position/pose** 没有数据，先不要继续试飞；检查动捕 UDP、**/mocap/odom -> /mavros/odometry/out** relay、静态 TF、MAVROS odometry 插件和 PX4 EKF/local position 状态。

预检查通过后停止上面的 launch，再正式启动执行器。正式启动后不要再在飞机旁边临时查话题，主要看终端日志：

roslaunch real_uav_bridge onboard_position_cmd_receiver_px4.launch
  drone_id:=0
  ground_ip:=<PC_IP>
  cmd_listen_port:=18000
  ground_port:=18100
  mocap_listen_port:=5005
  mocap_tracker:=<drone0_tracker>
  start_mavros:=true
  start_px4ctrl:=true
  relay_odom_to_mavros:=true
  fcu_url:=/dev/ttyTHS0:921600

正式启动预期日志：

1. **mocap_local_odom_to_mavros** 打印 local origin fixed。
2. **px4_position_executor** 打印 frame alignment fixed，其中包含 **world_origin** 和 **px4_local_origin**。
3. 起飞 setpoint 日志应显示 **takeoff local=(0.00, 0.00, 1.00) -> px4 local=(px4_local_origin.x, px4_local_origin.y, px4_local_origin.z + 1.00)**。
4. 预发送 100 个 setpoint 后自动切 **OFFBOARD**，然后自动 arm。
5. 起飞到启动点局部 **[0, 0, 1.0]** 后悬停，并发布 **/traj_start_trigger**。
6. 机载 bridge 把 **/traj_start_trigger** 回传到 PC 的 **/drone0/traj_start_trigger**。

PC 端确认 ready trigger：

rostopic echo -n1 /drone0/traj_start_trigger

单机试飞时没有三机 all-ready 节点，手动打开 command gate：

rostopic pub -1 /triger geometry_msgs/PoseStamped "header:
  stamp: now
  frame_id: 'world'
pose:
  orientation:
    w: 1.0"

PC 端发布一个定点 position command。这里的 **position 是世界系绝对点**，不要再固定写 0,0,1；先从 **/drone0/traj_start_trigger** 或 **/drone0/udp_odom** 读取当前 world 位置，填入当前位置或只加 0.1 到 0.2 m 的小偏移：

rostopic pub -r 20 /drone0/position_cmd quadrotor_msgs/PositionCommand "header:
  frame_id: 'world'
position: {x: <current_world_x>, y: <current_world_y>, z: <current_world_z>}
velocity: {x: 0.0, y: 0.0, z: 0.0}
acceleration: {x: 0.0, y: 0.0, z: 0.0}
jerk: {x: 0.0, y: 0.0, z: 0.0}
yaw: 0.0
yaw_dot: 0.0
kx: [0.0, 0.0, 0.0]
kv: [0.0, 0.0, 0.0]
trajectory_id: 1
trajectory_flag: 1"

机载端确认 command 到达并被转换为 PX4 local setpoint。安全上更推荐看执行器终端里的 **cmd world -> px4 local** 日志；台架或确认安全时再查话题：

rostopic hz /tracker_cmd/position_cmd
rostopic echo -n1 /tracker_cmd/position_cmd
rostopic echo -n1 /mavros/setpoint_raw/local

停止 PC 端定点 command 后，新控制节点会在 **cmd_timeout_sec=0.5s** 后回到当前位置 hover。降落/退出仍走遥控器或 QGC 的 PX4 模式切换与 disarm，不再依赖旧 px4ctrl 的 AUTO_HOVER/CMD_CTRL 状态机。

**三机端口建议**

drone0: mocap 5005, cmd 18000, ground 18100
drone1: mocap 5006, cmd 18001, ground 18101
drone2: mocap 5007, cmd 18002, ground 18102

**实际飞行
**

**0. IP 和端口约定**

PC_IP=<你的PC IP>

UAV0_IP=<drone0机载端IP>
UAV1_IP=<drone1机载端IP>
UAV2_IP=<drone2机载端IP>

drone0: mocap 5005, cmd 18000, ground 18100
drone1: mocap 5006, cmd 18001, ground 18101
drone2: mocap 5007, cmd 18002, ground 18102

**1. 动捕电脑启动 UDP relay**
每架无人机的动捕数据要同时发给 PC 和对应机载端。

python mocap_udp_relay.py --tracker <drone0_tracker> --target <PC_IP>:5005
python mocap_udp_relay.py --tracker <drone0_tracker> --target <UAV0_IP>:5005

python mocap_udp_relay.py --tracker <drone1_tracker> --target <PC_IP>:5006
python mocap_udp_relay.py --tracker <drone1_tracker> --target <UAV1_IP>:5006

python mocap_udp_relay.py --tracker <drone2_tracker> --target <PC_IP>:5007
python mocap_udp_relay.py --tracker <drone2_tracker> --target <UAV2_IP>:5007

**2. PC 启动真实场地规划链路**
第一轮建议先关掉 target evasion：

cd /home/xia_s1r/Documents/FormaitionVSTracker/Formation-Tracker
source devel/setup.bash

roslaunch simulation tracking_real_field_real_triangle.launch
  drone0_ip:=<UAV0_IP>
  drone1_ip:=<UAV1_IP>
  drone2_ip:=<UAV2_IP>
  drone0_mocap_tracker:=<drone0_tracker>
  drone1_mocap_tracker:=<drone1_tracker>
  drone2_mocap_tracker:=<drone2_tracker>
  start_target_evasion:=false
  send_position_cmd_before_trigger:=false
  rviz:=true

PC 检查：

rostopic hz /drone0/odom
rostopic hz /drone1/odom
rostopic hz /drone2/odom

rostopic hz /target/odom
rostopic hz /global_map

如果机载端已经启动，还可以看机载回传 odom：

rostopic hz /drone0/unused_udp_odom
rostopic hz /drone1/unused_udp_odom
rostopic hz /drone2/unused_udp_odom

**3. 三架机载端启动**
drone0：

cd /home/xia_s1r/Documents/FormaitionVSTracker/Formation-Tracker-Onboard
source devel/setup.bash

roslaunch real_uav_bridge onboard_position_cmd_receiver_px4.launch
  drone_id:=0
  ground_ip:=<PC_IP>
  cmd_listen_port:=18000
  ground_port:=18100
  mocap_listen_port:=5005
  mocap_tracker:=<drone0_tracker>
  start_mavros:=true
  start_px4ctrl:=true
  relay_odom_to_mavros:=true
  fcu_url:=/dev/ttyTHS1:921600

drone1：

roslaunch real_uav_bridge onboard_position_cmd_receiver_px4.launch
  drone_id:=1
  ground_ip:=<PC_IP>
  cmd_listen_port:=18001
  ground_port:=18101
  mocap_listen_port:=5006
  mocap_tracker:=<drone1_tracker>
  start_mavros:=true
  start_px4ctrl:=true
  relay_odom_to_mavros:=true
  fcu_url:=/dev/ttyTHS1:921600

drone2：

roslaunch real_uav_bridge onboard_position_cmd_receiver_px4.launch
  drone_id:=2
  ground_ip:=<PC_IP>
  cmd_listen_port:=18002
  ground_port:=18102
  mocap_listen_port:=5007
  mocap_tracker:=<drone2_tracker>
  start_mavros:=true
  start_px4ctrl:=true
  relay_odom_to_mavros:=true
  fcu_url:=/dev/ttyTHS1:921600

每架机载端检查：

rostopic hz /mocap/odom
rostopic echo -n1 /mocap/local_origin
rostopic hz /mavros/odometry/out
rostopic hz /mavros/local_position/pose
rosrun tf tf_echo odom camera_init
rosrun tf tf_echo body base_link
rostopic hz /tracker_cmd/position_cmd
rostopic echo /mavros/state

**/tracker_cmd/position_cmd** 在 **/triger** 前可以没有数据，这是正常的，因为 PC command gate 关闭。**/mavros/local_position/pose** 必须有数据，否则 PX4 没有可用 local position，执行器会停在等待 local pose。

**4. 起飞和接管顺序**

新的 onboard 控制方式不再使用旧 px4ctrl 的 **AUTO_HOVER/CMD_CTRL** 状态机，也不再通过 5/6 通道切换控制器。每架机载端启动 `start_px4ctrl:=true` 后，实际启动的是 **px4_position_executor**，它会自动完成：

1. launch 按 27com 的方式把 **/mocap/odom** 转成启动点局部 odom 后发到 **/mavros/odometry/out**，并发布锁存的 **/mocap/local_origin**。
2. 等待 **/mocap/odom**、**/mocap/local_origin**、**/mavros/state**、FCU connection 和 **/mavros/local_position/pose**。
3. 执行器冻结 **world_origin** 和 **px4_local_origin**，后续用 **px4_local_origin + (cmd_world - world_origin)** 把 PC 世界系 command 映射到 PX4 local setpoint。
4. 起飞目标按 27com 风格使用启动点局部 **[0, 0, 1.0]**，实际发布为 **px4_local_origin + [0, 0, 1.0]**。
5. 向 **/mavros/setpoint_raw/local** 预发送 100 个 PositionTarget。
6. 调用 **/mavros/set_mode** 切 **OFFBOARD**。
7. 调用 **/mavros/cmd/arming** 自动 arm。
8. 起飞到启动点局部 **[0, 0, 1.0]**。
9. 到达高度后延迟约 2 秒，发布 **/traj_start_trigger**。
10. 机载 bridge 回传 PC，PC 收齐三架 **/droneX/traj_start_trigger** 后自动发布 **/triger**。
11. PC command gate 打开，planning 开始发送世界系绝对 **/droneX/position_cmd**。
12. 机载端收到 **/tracker_cmd/position_cmd** 后转换成 **/mavros/setpoint_raw/local**，由 PX4 内部位置环执行。

PC 检查：

rostopic echo -n1 /triger
rostopic echo -n1 /drone0/traj_start_trigger
rostopic echo -n1 /drone1/traj_start_trigger
rostopic echo -n1 /drone2/traj_start_trigger
rostopic hz /drone0/position_cmd
rostopic hz /drone1/position_cmd
rostopic hz /drone2/position_cmd

机载端检查：

rostopic echo /mavros/state
rostopic hz /mavros/local_position/pose
rostopic hz /mavros/setpoint_raw/local
rostopic echo -n1 /mavros/setpoint_raw/local
rostopic hz /tracker_cmd/position_cmd

**/tracker_cmd/position_cmd** 在 **/triger** 前可以没有数据，这是正常的；**/mavros/setpoint_raw/local** 在起飞和 hover 阶段应该持续有数据。
起飞阶段 **/mavros/setpoint_raw/local.position.x/y** 应接近执行器日志里的 **px4_local_origin.x/y**，不能固定为 0；**position.z** 应接近 **px4_local_origin.z + 1.0**。收到规划 command 后，终端应出现 **cmd world -> px4 local** 的转换日志。

**5. 第二轮打开 target evasion**
第一轮确认三机能稳定进入程序接管后，再重启 PC launch：

roslaunch simulation tracking_real_field_real_triangle.launch
  drone0_ip:=<UAV0_IP>
  drone1_ip:=<UAV1_IP>
  drone2_ip:=<UAV2_IP>
  drone0_mocap_tracker:=<drone0_tracker>
  drone1_mocap_tracker:=<drone1_tracker>
  drone2_mocap_tracker:=<drone2_tracker>
  start_target_evasion:=true
  target_wait_for_trigger:=true
  target_post_trigger_delay:=3.0
  send_position_cmd_before_trigger:=false
  rviz:=true

这时 target evasion 会在 **/triger** 后再等 3 秒启动。

**应急动作**

* 遥控器或 QGC 切回 PX4 手动/定点/降落模式：退出 OFFBOARD，由飞控原生模式接管。
* 停止 PC 端 position command：新控制节点会在 **cmd_timeout_sec=0.5s** 后回到当前位置 hover。
* RC kill/disarm：最后一级硬停止，务必先做无桨测试。
